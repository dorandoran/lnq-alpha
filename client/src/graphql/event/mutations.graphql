#import "./fragments.graphql"

mutation CreateEvent(
  $id: String!
  $ownerId: String!
  $name: String!
  $type: String!
  $date: Date!
  $location: LocationInput!
  $url: String
  $description: String!
  $plusOne: Boolean!
  $isPrivate: Boolean!
) {
  createEvent(
    id: $id
    ownerId: $ownerId
    name: $name
    type: $type
    date: $date
    location: $location
    url: $url
    description: $description
    plusOne: $plusOne
    isPrivate: $isPrivate
  ) {
    ...eventFields
  }
}
